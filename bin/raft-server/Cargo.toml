[package]
name = "raft-server"
version = "0.1.0"
edition = "2021"

[dependencies]
api = { path = "../../crates/api" }
kv-store = { path = "../../crates/kv-store" }
raft-core = { path = "../../crates/raft-core" }
actix-web = "4"
chrono = "0.4"
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
